{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "description": "Azure Functions (Node.js, modelo de programação v4) - CRUD de pedidos (MongoDB Atlas) para a atividade formativa de Microfrontend.",
  "main": "src/functions/*.js",
  "scripts": {
    "start": "func start",
    "test": "echo \"(sem testes automatizados nesta atividade)\" && exit 0"
  },
  "dependencies": {
    "@azure/functions": "^4.5.1",
    "mongodb": "^6.10.0"
  }
}
